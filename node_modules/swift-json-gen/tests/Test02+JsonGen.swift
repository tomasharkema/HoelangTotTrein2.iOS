//
//  Test02+JsonGen.swift
//
//  Auto generated by swift-json-gen on Thu, 05 Nov 2015 07:19:20 GMT
//  See for details: https://github.com/tomlokhorst/swift-json-gen
//

import Foundation

extension Test02 {
  static func decodeJson(json: AnyObject) throws -> Test02 {
    guard let dict = json as? [String : AnyObject] else {
      throw JsonDecodeError.WrongType(rawValue: json, expectedType: "Object")
    }

    var errors: [String: JsonDecodeError] = [:]

    var one_optional: Int?
    var two_optional: String??
    var sub_optional: Sub?

    if let one_field: AnyObject = dict["one"] {
      do {
        one_optional = try MyInt.decodeJson(one_field)
      }
      catch let error as JsonDecodeError {
        errors["one"] = error
      }
    }
    else {
      errors["one"] = JsonDecodeError.MissingField
    }

    let two_field: AnyObject? = dict["two"]
    if let two_field = two_field where !(two_field is NSNull) {
      do {
        two_optional = try Optional.decodeJson({ try String.decodeJson($0) }, two_field)
      }
      catch let error as JsonDecodeError {
        errors["two"] = error
      }
    }
    else {
      two_optional = .Some(nil)
    }

    if let sub_field: AnyObject = dict["sub"] {
      do {
        sub_optional = try Sub.decodeJson(sub_field)
      }
      catch let error as JsonDecodeError {
        errors["sub"] = error
      }
    }
    else {
      errors["sub"] = JsonDecodeError.MissingField
    }

    guard
      let one = one_optional,
      let two = two_optional,
      let sub = sub_optional
    else {
      throw JsonDecodeError.StructErrors(type: "Test02", errors: errors)
    }

    return Test02(one: one, two: two, sub: sub)
  }

  func encodeJson() -> [String: AnyObject] {
    var dict: [String: AnyObject] = [:]

    dict["one"] = one.encodeJson()
    dict["two"] = two.encodeJson({ $0.encodeJson() })
    dict["sub"] = sub.encodeJson()

    return dict
  }
}

extension Sub {
  static func decodeJson(json: AnyObject) throws -> Sub {
    guard let dict = json as? [String : AnyObject] else {
      throw JsonDecodeError.WrongType(rawValue: json, expectedType: "Object")
    }

    var errors: [String: JsonDecodeError] = [:]

    var one_optional: NSURL?
    var two_optional: NSDate?

    if let one_field: AnyObject = dict["one"] {
      do {
        one_optional = try NSURL.decodeJson(one_field)
      }
      catch let error as JsonDecodeError {
        errors["one"] = error
      }
    }
    else {
      errors["one"] = JsonDecodeError.MissingField
    }

    if let two_field: AnyObject = dict["two"] {
      do {
        two_optional = try NSDate.decodeJson(two_field)
      }
      catch let error as JsonDecodeError {
        errors["two"] = error
      }
    }
    else {
      errors["two"] = JsonDecodeError.MissingField
    }

    guard
      let one = one_optional,
      let two = two_optional
    else {
      throw JsonDecodeError.StructErrors(type: "Sub", errors: errors)
    }

    return Sub(one: one, two: two)
  }

  func encodeJson() -> [String: AnyObject] {
    var dict: [String: AnyObject] = [:]

    dict["one"] = one.encodeJson()
    dict["two"] = two.encodeJson()

    return dict
  }
}
